spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://casestudy-config-service.default:8888
---
spring:
  profiles: development
  cloud:
    config:
      uri: http://localhost:8888
